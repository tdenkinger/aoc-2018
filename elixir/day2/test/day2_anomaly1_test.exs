defmodule Day2Anomaly1Test do
  use ExUnit.Case

  test "does stuff" do
    target_tokens = [{"b", 3}, {"c", 2}]
  end

  # test "finds counts equal to 2" do
  #   occurrences = %{"a" => 1, "b" => 3, "c" => 2}

  #   twos = Day2Anomaly1.filter_occurrences(occurrences)

  #   assert twos == [{"b", 3}, {"c", 2}]
  # end

  # test "counts occurrences of tokens" do
  #   tokens = ["a", "b", "c", "c"]

  #   occurrences = Day2Anomaly1.count_occurrences(tokens)

  #   assert occurrences == %{"a" => 1, "b" => 1, "c" => 2}
  # end
end
